import { Metadata } from "next"

export const metadata: Metadata = {
  title: "Contact GalleryHub - Get in Touch",
  description: "Contact GalleryHub for support, partnerships, or any inquiries about our digital art gallery platform.",
}

export default function ContactLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
} 